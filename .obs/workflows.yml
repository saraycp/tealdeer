workflow:
  steps:
    - link_package:
        source_project: home:SCabreraPadron
        source_package: tealdeer
  filters:
    architectures:
      only:
        - x86_64
    repositories:
      only:
        - openSUSE_Tumbleweed
